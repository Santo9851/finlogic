// Static metadata for pages that are "use client" components
// Next.js requires metadata to be exported from Server Components.
// For "use client" pages, export metadata from a separate layout.js
// co-located with the page OR from the page itself if converted to a server component.
// This file documents the per-page metadata used via layout.js siblings.

// NOTE: Since most pages use "use client", metadata is exported from
// co-located layout.js files created alongside each page.
